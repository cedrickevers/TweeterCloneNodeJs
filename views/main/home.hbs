<div class="starter-template">
    <p class="lead">what is happ ?</p>
</div>

<div class="col-md-6 col-md-offset-3">
    <form  id="sendTweet">
        <input type="text" class="form-control" id="tweet" name="">
        <br>
        <button class="btn btn-primary">Tweet</button>

        <br>
        <br>

        <div id="tweets">
            <div class="media">
                <div class="media-left">
                    <a href="#"><image class="media-object" src="{{user.photo}}"></a>
               </div>
               <div class="media-body">
                   <h4 class="media-heading">{{user.name}}</h4>
                   <p>Wesh 'i m Miakis</p>
               </div>    
            </div>
        </div>
    </form>
</div>